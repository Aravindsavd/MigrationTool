{
  "name": "migration-tracker",
  "version": "1.0.0",
  "description": "On-Prem to SaaS Migration Tracker - HKakshatha Portfolio",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node -e \"const app = require('./src/server'); process.exit(0);\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
